---
tags:
  - 주식
  - 도구
  - Python
  - yfinance
created: 2025-02-20
---

# 주식 정보·차트 뷰어 기록

## 개요

주식 **티커 심볼**을 입력하면 해당 종목의 **기본 정보**와 **가격 차트**를 보여주는 Python 도구를 만들었음.

- **위치**: 옵시디언 볼트의 `Stock` 폴더(개발용). 공개 저장소는 [karin01/MyStock](https://github.com/karin01/MyStock) 참고.
- **데이터 출처**: Yahoo Finance (yfinance 라이브러리, 무료·API 키 불필요)

## 구성 파일

| 파일 | 설명 |
|------|------|
| `backend/stock_viewer.py` | 핵심 로직: 정보 조회, 히스토리 조회, 차트 저장 |
| `frontend/app_streamlit.py` | 웹 UI (Streamlit) — 브라우저에서 조회 |
| `requirements.txt` | 필수 패키지 목록 |
| `README.md` | 사용법 안내 |

## 사용 방법

### 1) 명령줄

```bash
cd <MyStock 프로젝트 루트>
pip install -r requirements.txt
python backend/stock_viewer.py AAPL
python backend/stock_viewer.py 005930.KS 1y
```

- 터미널에 요약 정보 출력
- 같은 폴더에 `차트_티커.png` 저장

### 2) 웹 UI (선택)

```bash
pip install streamlit
streamlit run frontend/app_streamlit.py
```

- 브라우저에서 티커 입력 후 정보·차트 확인

## 티커 참고

- 미국: `AAPL`, `MSFT`, `GOOG` 등
- 한국: `005930.KS`(삼성전자), `035420.KS`(네이버) — 코스피 `.KS`, 코스닥 `.KQ`

## 기술 요약

- **get_stock_info(ticker)**: 현재가, 시가총액, PER, 섹터 등
- **fetch_history(ticker, period)**: OHLC 과거 데이터 (period: 1d, 1mo, 1y 등)
- **draw_chart(ticker, period, save_path)**: matplotlib으로 차트 이미지 저장
- 에러 처리: 패키지 없음, 데이터 없음, 잘못된 티커 등 처리

## 주의사항

- yfinance는 비공식 API이며, 참고·학습용으로 사용.
- 투자 판단은 본인 책임.

## 관련 링크

- [yfinance 문서](https://ranaroussi.github.io/yfinance)
- Yahoo Finance 데이터는 개인용으로만 사용 권장

---

## 2025-02-20: 거래 현황 기능 추가

### 추가 내용
- **trading_overview.py**: pykrx 기반 국내 시장 거래 현황 모듈
  - `get_market_overview()`: 코스피/코스닥 거래대금, 상승/하락/보합 종목 수
  - `get_top_traded_stocks()`: 거래대금 상위 종목 (코스피/코스닥)
  - `get_top_gainers_losers()`: 등락률 상위·하위 종목
- **app_streamlit.py**: "거래 현황 (국내 시장)" expander 추가
  - 시장 요약: 코스피/코스닥 거래대금(조원), 상승/하락/보합 종목 수
  - 거래대금 상위: 코스피·코스닥 탭별 테이블
  - 등락률 상위/하위: 상승률·하락률 상위 5종목

### 데이터 출처
- pykrx `get_market_ohlcv_by_ticker(날짜, market)` — API 키 불필요

---

## 2025-02-20: 보유 종목·손익 추적 기능 추가

### 추가 내용
- **portfolio.py**: 구매 등록·손익 계산 모듈
  - `add_purchase()`: 종목, 수량, 매수가, 매수일, 메모 저장
  - `delete_purchase()`: 구매 내역 삭제
  - `get_holdings_with_profit_loss()`: 티커별 합산 + 현재가·손익·손익률 계산
  - 저장: `portfolio_holdings.json` (Stock 폴더)
- **app_streamlit.py**: "내 보유 종목" expander
  - 구매 등록 폼: 종목명/티커, 수량, 매수가, 매수일
  - 총 매수금액·평가금액·손익 요약
  - 종목별 수량, 구매단가(원), 현재가, 평가금액, 손익(률) 테이블
  - 구매 내역 상세(삭제 버튼)

---

## 2025-02-23: 보유 종목 현재가 미표시 버그 수정

### 문제
- 장이 열려 있는데도 보유 종목 중 일부(예: KG모빌리티, SGA솔루션즈, 펄어비스 등)의 현재가·평가금액·손익이 "-"로 표시됨
- `451060.KS`, `AMD`처럼 티커 형식으로 등록한 종목만 정상 표시됨

### 원인
- `portfolio_holdings.json`에 **종목명**(한글)으로 저장된 경우가 있음 (예: "KG모빌리티", "디렉시온 반도체 3배 ETF")
- `_현재가_및_통화_조회()`가 이 값을 그대로 `get_stock_info()`에 전달
- yfinance/pykrx API는 **티커 심볼**(예: `003620.KS`, `SOXL`)을 필요로 함
- 종목명을 티커로 변환하는 `resolve_to_ticker()`를 호출하지 않아 조회 실패

### 수정 내용
- **app_streamlit.py** `_현재가_및_통화_조회()` 함수 수정
  - `resolve_to_ticker(ticker)`로 종목명 → 티커 변환 후 조회
  - 예: "KG모빌리티" → "003620.KS", "디렉시온 반도체 3배 ETF" → "SOXL"

---

## 2025-02-23: 디지털 시계 실시간 자동 갱신

### 추가 내용
- **app_streamlit.py**: 코스피·코스닥·환율 디지털 시계 위젯을 **60초마다 자동 갱신**
  - `@st.fragment(run_every=timedelta(seconds=60))` 사용
  - 페이지 새로고침 없이 해당 영역만 주기적으로 재조회
- **requirements.txt**: streamlit>=1.33.0 (fragment run_every 기능 필요)

---

## 2025-02-23: 보유 종목 클릭 → 조회 영역 자동 표시

### 추가 내용
- **app_streamlit.py**: 보유 종목 테이블에서 **행 클릭** 시 아래 주식 조회 영역에 해당 종목 자동 표시
  - `st.dataframe(..., on_select="rerun", selection_mode="single-row")` 사용
  - 선택된 행의 티커를 `suggested_ticker`로 전달 → 조회 결과 표시
  - 안내 문구: "종목 행을 클릭하면 아래 조회 영역에 자동으로 표시됩니다"
- **requirements.txt**: streamlit>=1.35.0 (dataframe 행 선택 기능 필요)

---

## 2025-02-23: 로그인 기능 추가 — 본인 보유 종목만 표시

### 추가 내용
- **auth.py**: 로그인/회원가입 모듈
  - `register(username, password)`: 회원가입 (아이디 2자 이상, 비밀번호 4자 이상)
  - `login(username, password)`: 로그인 검증
  - 비밀번호 PBKDF2-SHA256 해싱 (hashlib, secrets — 추가 패키지 불필요)
  - 사용자 정보: `users.json` (로컬 저장)
- **portfolio.py**: 사용자별 데이터 분리
  - `add_purchase(user_id, ...)`, `delete_purchase(user_id, id)`, `get_holdings(user_id)`, `get_holdings_with_profit_loss(user_id, ...)`
  - 저장 형식: `{ "user1": [...], "user2": [...] }`
  - **기존 데이터 마이그레이션**: 구 형식(배열) → `"default"` 사용자로 자동 이전
- **app_streamlit.py**: 로그인 UI
  - 미로그인: 로그인/회원가입 탭 표시
  - 로그인 후: 본인 보유 종목만 표시, 구매 등록·삭제 가능
  - 로그아웃 버튼
- **.gitignore**: `users.json` 추가 (비밀번호 해시 포함)

---

## 2025-02-23: 내 보유 종목 섹션 60초마다 자동 갱신

### 추가 내용
- **app_streamlit.py**: "내 보유 종목 (구매 등록·손익)" 섹션의 손익 표시를 **60초마다 자동 갱신**
  - `@st.fragment(run_every=timedelta(seconds=60))`로 보유종목_손익_표시() 래핑
  - 현재가·환율 반영된 총 매수금액·평가금액·손익이 주기적으로 업데이트됨
  - 구매 등록 폼은 fragment 밖에 있어 입력 중 리셋되지 않음

---

## 2025-02-25: 로그인/로그아웃 시 국내 거래 현황 리로드 방지

### 문제
- 로그인·로그아웃할 때마다 `st.rerun()`으로 전체 앱이 리로드됨
- 국내 거래 현황(거래대금 상위, 등락률 등)까지 매번 다시 불러와 로딩 시간이 길어짐

### 수정 내용
- **app_streamlit.py**: "내 보유 종목" 섹션 전체를 `@st.fragment`로 감쌈
  - 로그인·로그아웃·회원가입·구매 등록·삭제 시 해당 fragment만 리로드
  - 국내 거래 현황·지수 차트 등 무거운 영역은 리로드하지 않음
  - `st.rerun()` 호출 제거 → fragment 상호작용 시 자동으로 해당 fragment만 재실행

---

## 2025-02-25: 회원가입 창 보안 취약점 수정

### 문제
- "기존에 사용하시던 데이터가 있다면, 'default'로 회원가입 후 로그인하면 해당 데이터를 볼 수 있습니다" 문구 노출
- 로그인 도입 전 데이터는 `portfolio_holdings.json`에서 `"default"` 키로 마이그레이션됨
- **누구나** `default`로 회원가입하면 타인의 보유 종목 데이터에 접근 가능

### 수정 내용
- **app_streamlit.py**: 해당 안내 문구 제거

---

## 2025-02-25: 거래 현황 5시간 캐시

### 추가 내용
- **app_streamlit.py**: "거래 현황 (국내 시장)" 섹션에 `@st.cache_data(ttl=5*60*60)` 적용
  - KRX(pykrx) API 호출을 5시간마다 1회로 제한
  - 페이지 새로고침·탭 전환 시에도 캐시된 데이터 사용

---

## 2025-03-02: 종목 비교 차트 기능 추가

### 추가 내용
- **app_streamlit.py**: "🔄 종목 비교 차트" expander 추가
  - 2~3개 종목을 한 차트에 정규화(시작일 100 기준)하여 수익률 비교
  - 국내·해외 지수 비교와 동일한 로직 재사용 (`fetch_history` → 정규화 → `line_chart`)
  - 입력: 종목명 또는 티커 (예: 삼성전자, 하이닉스, AAPL)
  - 기간 선택: 1mo, 3mo, 6mo, 1y, 2y

---

## 2025-03-02: 최근 조회·뉴스·배당·관심종목·포트폴리오 차트·목표가 알림 추가

### 1. 최근 조회 종목
- **app_streamlit.py**: `st.session_state.recent_tickers`로 최근 조회한 종목 저장 (최대 10개)
- 조회 성공 시 자동 추가, 클릭 시 해당 종목 바로 조회

### 2. 뉴스·배당 정보
- **stock_news.py** (신규): yfinance 기반
  - `get_stock_news(ticker, limit)`: 관련 뉴스 제목·URL
  - `get_dividend_info(ticker)`: 배당 수익률, 최근 배당, 배당성향
- **app_streamlit.py**: 종목 조회 시 체크박스로 뉴스/배당 표시

### 3. 관심종목(워치리스트)
- **watchlist.py** (신규): `watchlist.json` 저장, 사용자별 분리
  - `get_watchlist`, `add_to_watchlist`, `remove_from_watchlist`
- **app_streamlit.py**: 로그인 사용자 전용 "⭐ 관심종목" expander
  - 현재가 표시, 클릭 시 조회, 삭제 버튼
  - 종목 상세 화면에서 "관심종목에 추가" 버튼

### 4. 포트폴리오 비중 차트
- **app_streamlit.py**: "내 보유 종목" 내 "📊 보유 비중 차트" expander
  - matplotlib 파이 차트로 종목별 평가금액 비중 표시

### 5. 목표가·알림 설정
- **alerts.py** (신규): `price_alerts.json` 저장, 사용자별 분리
  - `get_alerts`, `add_alert`, `delete_alert`
  - 티커, 목표가, 방향(이상/이하)
- **app_streamlit.py**: 로그인 사용자 전용 "🔔 목표가·알림 설정" expander
  - 도달 여부 표시 (현재가와 비교)
  - 종목 상세 화면에서 "목표가 알림 설정" (popover)

### .gitignore
- `watchlist.json`, `price_alerts.json` 추가 (개인 데이터)

---

## 2025-03-02: 대시보드 디자인 개선 + Firebase 인증

### 1. 대시보드 UI 개선
- **.streamlit/config.toml** (신규): 테마 설정 (primaryColor, backgroundColor 등)
- **app_streamlit.py**: 커스텀 CSS 적용
  - 카드형 Expander, 디지털 시계 스타일 개선
  - 사이드바·메트릭·버튼 스타일
  - 여백·타이포 정리

### 2. Firebase Firestore 인증
- **auth_firebase.py** (신규): Firestore 기반 회원가입·로그인
  - `users` 컬렉션에 username → password_hash 저장
  - FIREBASE_CREDENTIALS_PATH 또는 secrets.toml 설정
- **auth.py** 수정: Firebase 설정 시 Firestore 사용, 미설정 시 로컬 users.json 사용
- **API_설정_안내.md**: Firebase 설정 방법 추가

---

## 2025-03-02: 상용화 대비 기능 보강

### 1. 비밀번호 강화
- **auth.py**: `validate_password_strength()` — 8자 이상, 영문+숫자 필수
- 회원가입 시 복잡도 검사 적용 (Firebase/로컬 공통)

### 2. 포트폴리오 리스크·분산도
- **portfolio_analysis.py** (신규): `get_diversity_score()`, `get_concentration_ratio()`
- 보유 종목 섹션에 분산도 점수(0~100), 상위 3종목 집중도 표시

### 3. UI/UX
- **빠른 사용법** expander 추가
- 비밀번호 placeholder·help 수정 (8자, 영문+숫자)
- 보유 종목 없을 때 입력 팁 안내

---

## 2025-03-02: 상용화 부족분 보강

### 1. 차별화 (종목 스크리너)
- **stock_screeners.py** (신규): 배당률 상위, 저PER(15 이하) 스크리닝
- **app_streamlit.py**: "종목 스크리너 (배당주·저PER)" expander

### 2. 데이터 안정성
- **data_sources.py**: get_stock_info(yfinance) 실패 시 1회 자동 재시도
- 기본 정보 영역에 "데이터: Yahoo Finance, KRX 등" 출처 표시

### 3. 실사용 (리밸런싱·매도 시뮬)
- **portfolio_analysis.py**: `get_rebalance_suggestions()` — 균등 비중 기준 매수/매도 제안
- **fee_tax.py**: `simulate_sell()` — 매도가 입력 시 수수료·세금 반영 순손익
- 보유 종목에 "리밸런싱 제안", "매도 시뮬레이터" expander 추가

---

## 2025-03-02: 수수료·세금 대략 계산 기능 추가

### 추가 내용
- **fee_tax.py** (신규): 수수료·세금 근사 계산
  - `estimate_sell_cost_krw`: 매도 시 증권거래세(코스피 0.20%/코스닥 0.18%), 투자자보호기금 0.15%, 증권사 수수료 0.02%
  - `estimate_buy_cost_krw`: 매수 시 수수료
  - `estimate_capital_gains_tax_krw`: 양도소득세 (연 2.5천만원 비과세, 초과분 22%)
  - `estimate_holdings_sell_summary`: 보유 종목 전체 매도 시 예상 비용
- **app_streamlit.py**: "내 보유 종목" 내 "🧮 예상 수수료·세금 (매도 시)" expander
  - 매도 예상 총액, 매도 수수료·거래세, 양도소득세(참고) 표시
  - ※ 국내주식 기준, 참고용

---

## 2026-03-06: 구매단가 문구·원화/달러 표기 정리

### 변경 사항
1. **평균단가 → 구매단가**
   - 보유 종목 테이블: "평균매수가(원)" → **"구매단가(원)"** (app_streamlit.py)
   - HTML 포트폴리오 테이블: "평균단가" → **"구매단가"** (frontend/index.html)
   - 의미: 같은 종목 여러 번 매수 시 평균 단가이지만, 컬럼명은 "구매단가"로 통일

2. **원화/달러 표기 통일**
   - **_금액_포맷()**: 원화 → `1,234원 (KRW)`, 달러 → `$12.34 (USD)` 형식으로 통일
   - 기본 정보 탭 "현재가·전일 종가": 위 포맷 적용해 통화 혼동 방지
   - 보유 종목·관심종목·매도 시뮬 등 모든 금액 표시에 동일 적용

3. **현재가 48원 관련**
   - 현재가는 Yahoo Finance / pykrx(KRX) 기준으로 조회됨. 48원인 종목(저가주·우선주 등)이 있을 수 있음.
   - **표시 시 항상 통화를 붙이도록** 수정해, "48"만 보이던 상황을 없앰 → "48원 (KRW)" 또는 "$48.00 (USD)"로 구분 가능.

### 2026-03-06 추가: 미국 주식 달러(USD) 표기 수정
- **원인**: yfinance가 한국 로케일/환경에서 미국 주식(AMD, SOXL, RXT 등)에 `currency: "KRW"`를 반환해, 현재가가 "192원", "48원"처럼 원화로 잘못 표시됨.
- **수정**:
  - **data_sources.get_stock_info()**: 티커가 한국(.KS/.KQ)이 아니면 반환 정보의 `currency`/`currency_label`을 강제로 `USD` / `달러(USD)`로 설정.
  - **app_streamlit._현재가_및_통화_조회()**, **main.py _현재가_및_통화_조회()**: API 통화 값과 관계없이 미국 티커면 통화를 `달러(USD)`로 고정. fallback 경로에서도 `조회용_티커` 기준으로 원/달러 구분.
- 결과: AMD·디렉시온 반도체 3배·렉스페이스 등 미국 주식 현재가가 **$192.00 (USD)**, **$48.00 (USD)** 형태로 표시됨.

### 2026-03-06 추가: 조선·원유(정유) 회사명 매핑
- **backend/stock_viewer.py** `COMPANY_NAME_TO_TICKER`에 추가:
  - **조선·해양**: 대한조선(439260.KQ), STX조선해양/STX(067250.KQ), 현대미포조선/현대미포(010620.KS), 한진중공업/한진(097230.KS, HJ중공업), 대선조선(042660), HD현대중공업(329180), HD한국조선해양(009540), 대우조선(042660) 등.
  - **원유·정유·에너지**: SK에너지(096770), GS에너지(078930). (SK이노베이션·에스오일·S-Oil·GS칼텍스는 기존 매핑 유지, GS칼텍스 078930.KS로 이미 수정됨.)
  - **미국 원유·에너지**: 엑손모빌(XOM), 쉐브론(CVX), 코노코필립스(COP), BP, 쉘(SHEL), 마라톤오일(MRO), 헤스(HES), 오키드피닉스(OXY).
- 현대오일뱅크는 비상장이라 매핑에서 제외.

### 2026-03-06 추가: 웹 푸터 제작자 표기
- **frontend/index.html**: 메인 콘텐츠 하단에 `<footer class="app-footer">` 추가, `"주식 정보·차트 뷰어"`와 함께 **`<제작자 Luna>`** 텍스트 노출.
- **frontend/style.css**: `.app-footer`, `.footer-title`, `.footer-author` 스타일 추가 — 상단 보더, 살짝 투명한 텍스트, 제작자 이름은 포인트 컬러(초록)로 강조.

### 2026-03-06 추가: 산업 전망 자동 한글 번역
- **backend/stock_detail.py**:
  - `get_stock_detail()` → `_build_industry_outlook()` 경로에서 `longBusinessSummary`를 가져온 뒤, 새 함수 `_translate_summary_to_korean()`으로 한국어 번역 시도.
  - 번역 로직:
    - 문자열에 한글이 이미 있으면 그대로 사용 (`_has_korean()` 체크).
    - `OPENAI_API_KEY`가 설정되어 있으면 OpenAI `gpt-4o-mini`로 "영문 사업 요약 → 자연스러운 한국어" 번역.
    - API 키 없거나 에러 발생 시 원문(영어) 그대로 사용.
  - 번역이 실제로 수행된 경우 `industry_outlook["note"]`에
    `"영문 사업 요약을 자동 번역한 내용입니다. (정확한 투자 판단은 원문 및 공시를 참고하세요.)"` 문구를 추가.

### 2026-03-06 추가: 거래순위 TOP50 버튼·섹션
- **frontend/index.html**: 검색 영역에 버튼 "📊 거래순위 TOP50" 추가. 클릭 시 "거래대금 순위 TOP50 (코스피 25 + 코스닥 25)" 전용 섹션 표시/숨김 토글.
- **frontend/app.js**: 버튼 클릭 시 코스피 25 + 코스닥 25 거래대금 상위 종목을 `/api/market/top_traded`로 병렬 조회 후 테이블 렌더링. 테이블 행 클릭 시 해당 티커(.KS/.KQ)로 검색창에 넣고 조회 실행 → 차트·기본정보·재무 등 표시.
- **frontend/style.css**: `.btn-top50`, `.top50-section`, `.top50-table`, `.top50-row` 등 스타일 추가. 행 호버 시 강조, 등락률 색상(상승 녹색/하락 빨강) 적용.
- 데이터: 기존 `backend/trading_overview.get_top_traded_stocks(limit, market)` 및 `GET /api/market/top_traded` 활용 (pykrx 거래대금 기준).

### 2026-03-06 추가: 텔레그램 봇 연동
- **telegram_bot.py** (Stock 루트): 텔레그램 Bot API로 메시지 수신·응답.
  - 명령: `/start`, `/help` — 사용법 안내 / `/top50` — 거래대금 순위 TOP50 (코스피 25 + 코스닥 25) / `/search 종목명 또는 티커` — 해당 종목 현재가·시가총액·섹터 요약.
  - 토큰: `.env` 파일의 `TELEGRAM_BOT_TOKEN` 또는 환경변수 사용. `.env`는 `.gitignore`에 포함되어 저장소에 올라가지 않음.
- **텔레그램봇_실행.bat**: 봇 단독 실행용. `python telegram_bot.py` 호출.
- **requirements.txt**: `requests`, `python-dotenv` 추가.
- **.env.example**: 토큰 설정 예시 (실제 토큰은 `.env`에 로컬로 보관).

### 2026-04-07: 회원가입·시장지표 `Failed to fetch` (localhost vs 127.0.0.1, CORS)
- **현상**: 콘솔 `TypeError: Failed to fetch`, 코스피/코스닥/환율 "불러오는 중…" 고정, 회원가입·로그인 알림만 뜸.
- **원인**: `app.js` 의 `API_BASE` 가 `http://localhost:8000` 이면 Windows 에서 `localhost` → **IPv6(::1)** 만 시도하는 경우가 있어, **IPv4(127.0.0.1:8000)** 에만 떠 있는 FastAPI 에 연결 실패. 또 `CORSMiddleware` 에 `allow_credentials=True` 와 `allow_origins=["*"]` 조합은 스펙상 문제가 될 수 있음.
- **조치**: `API_BASE` → `http://127.0.0.1:8000/api` 고정, 연결 실패 시 `alertApiUnreachable()` 안내. `main.py` CORS 에 로컬 프론트 출처 명시. `index.html` `app.js` 쿼리로 캐시 무효화.
- **사용자 확인**: **백엔드 uvicorn 8000** 기동 후 `http://127.0.0.1:8000/docs` 확인. 프론트는 **`http://127.0.0.1:8765`** (정적 서버 포트).

### 2026-04-07 추가: 프론트 정적 서버 포트 8765
- **변경**: `7654` / `8080` 로 쓰던 **웹 UI(http.server)** 를 **`8765`** 로 통일 (`run_frontend.bat`, `run_stock.ps1`, `실행하기.bat`, `실행_명령어.txt`, `main.py` CORS, `app.js` 안내 문구).
- **API**: 여전히 **`http://127.0.0.1:8000`** (백엔드와 프론트는 포트를 나눠 씀).

### 2026-04-09: 거래량 순위 표시 + API `sort_by`
- **원인**: 웹에는 거래대금 TOP50만 있고, 백엔드도 거래대금 정렬만 함 → 거래량 순위가 안 나옴.
- **조치**: `trading_overview.get_top_traded_stocks(..., sort_by=)` — `volume`/`거래량` 시 거래량 컬럼으로 정렬, 응답에 `거래량` 필드 포함. yfinance 폴백도 동일. `GET /api/market/top_traded?sort_by=volume`. `index.html` TOP50 섹션 아래 **거래량 순위** 테이블 추가, `app.js` 에서 TOP50 열 때 거래대금·거래량 API 병렬 호출 후 렌더.

### 2026-04-09 추가: TOP50 등락률이 전부 +0.00%
- **원인**: pykrx 실패 시 **yfinance 폴백** `_get_top_traded_yfinance_fallback` 에서 `등락률` 을 **상수 0.0** 으로 넣고 있었음.
- **조치**: yfinance `history` 의 **전일·금일 종가**로 등락률(%) 계산. pykrx 경로는 `등락률` 컬럼을 `_col(..., 6)` 로 `_등락률` 에 복사 후 행에서 읽어 iloc 밀림을 줄임.
